name: CI SearchParameter Testing

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]
  workflow_dispatch:

jobs:
  CI_SEARCH_PARAM_TEST:
    uses: ./.github/workflows/ci_search_param_tests.yml
